<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      AppCompat parent is required: MainActivity extends AppCompatActivity
      (per-app locales) and calls installSplashScreen().
    -->
    <style name="Theme.CrimSys" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/clay_background</item>
    </style>
</resources>
