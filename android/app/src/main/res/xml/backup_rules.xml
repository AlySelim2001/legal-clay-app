<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- The Room DB is SQLCipher-encrypted; the key lives in the Keystore and is excluded. -->
    <exclude domain="database" path="crimsys.db" />
    <exclude domain="sharedpref" path="crimsys_db_key_prefs.xml" />
</full-backup-content>
