# PHASE1_FINAL.md — خلاصة المرحلة 1 (بناء واستعادة CI) — 2026-09-19

> **الفرع المنطقي:** `chore/phase-1-build-and-ci-recovery` (النسخ تُدار بمزامنة المنصة — git محجوب في بيئة التنفيذ، قيد مسجل §F في سجل الأوامر).
> **قاعدة الذهبية الملتزمة:** كل ما يلي مدعوم بأوامر منفذة فعليًا في هذه البيئة؛ كل ما لم يُنفَّذ معلن بحالته (NOT RUN / BLOCKED / بانتظار) دون أي ادعاء نجاح.

---

## 1. الخلاصة التنفيذية — حالة الأسطح الأربعة

| السطح | الحالة | الإثبات | ما يفصله عن "مكتمل" |
|---|---|---|---|
| **Web (React/Convex)** | ✅ **نظيف بالكامل محليًا** | `tsc` exit 0 · `lint` exit 0 (**46→0 أخطاء**) · lint **بوابة صلبة** في CI | لا شيء محليًا — تأكيد Run #120 شكلي |
| **Android (Kotlin/Gradle)** | ◐ **مستعاد ساكنًا** | عائق Run #119 (script imports) مُصلَح ومتحقق ساكنًا + مسح نمط الخطأ على كل السكربتات | **Run #120** هو الإثبات الوحيد — أي طبقة Kotlin/KSP/Hilt أعمق تُعالج بعد ظهورها في السجل الخام |
| **Room migration 4→5** | ◐ **عقد مكتمل + اختبار مكتوب** | تحليل DDL/كيانات/فهارس + `RoomMigration4To5Test.kt` (3 اختبارات) على القرص | `5.json` من KSP (ممنوع يدويًا) + تشغيل الاختبار — كلاهما بعد نجاح البناء |
| **Flutter** | ◐ **تحليل كامل، صفر تعديل** | جرد استيرادات فعلي: 13 حزمة مستخدمة-غير معلنة + 3 اختبارات بأسماء حزم خاطئة + export مكسور | **موافقتك** على جدول الحزم ثم بيئة Flutter للتحقق |

---

## 2. مصفوفة الأدلة المنفذة (محليًا، هذه الجلسة)

| الأمر | النتيجة | الحكم |
|---|---|---|
| `bun tsc -b --noEmit` | exit 0 (بعد كل دفعة تعديلات) | PASS |
| `bun run lint` | قبل: exit 1 / 46 errors · بعد: exit 0 / **0 errors / 25 warnings** | PASS |
| `bunx eslint . --format json` | تصنيف كل خطأ: ملف:سطر:عمود + قاعدة | أداة إثبات |
| فحص ساكن `android/app/build.gradle.kts` | imports صحيحة + `load` مرتبطة بمستقبِل | مطابق لإصلاح P1-A |
| مسح `*.gradle.kts` لنمط الخطأ | مسارات البناء نظيفة؛ استثناء `patch-kit/` غير المشار إليه | موثق |
| grep للمعرفات المحذوفة (labelFor/isoInDays/handleSessionCreated/useMutation…) | صفر مراجل معلقة | محفوظ-السلوك |
| فحص `main.yml` البنيوي | `ESLint (hard gate)` بلا أقنعة + `build-android needs [quality, android-unit]` | بوابة فعالة |

**غير قابل للتنفيذ هنا (قيد بيئي موثق بأدلة حية في RUN_120_EVIDENCE §1):** git/gh/CI، Java/Android SDK، Flutter SDK، قراءة البيئة.

---

## 3. فهرس التسليمات

| الملف | المحتوى |
|---|---|
| `AUDIT_REPORT.md` | التدقيق الأولي الشامل (المرحلة 0) |
| `FLUTTER_GAP_REPORT.md` | جرد استيرادات Flutter: معلن/غير معلن/مفقود/غير مستخدم + جدول الموافقة المرجعي §5 |
| `FLUTTER_APPROVAL_MATRIX.md` | **ملف القرار**: 13 حزمة (إصدار/ترخيص/حاجة/توافق/بديل/مخاطر/أثر منصات/حجم) — 0 ACCEPT نهائي · 3 ACCEPT-جاهز · 10 DEFER + ملحقا Patch للاختبارات والـexport (عرض فقط، لا يُطبق قبل الموافقة) |
| `TEST_EXECUTION_MATRIX.md` | كل اختبار وأمره وبيئته ونتيجته الحقيقية أو سبب عدم التشغيل |
| `PHASE1_COMMAND_LOG.md` | السجل الخام الكامل للأوامر والنتائج + §I تنظيف lint |
| `RUN_120_EVIDENCE.md` | أدلة محاولات الإطلاق (حجب ثلاثي) + جدول تصنيف طبقات الفشل + قالب الأدلة الجاهز للامتلاء |
| `ROOM_MIGRATION_4_5_REPORT.md` | عقد 4→5 كاملًا + الاختبار المكتوب + شروط فك حجب 5.json |
| `TECHNICAL_DEBT.md` | التحذيرات الـ25 (ملف/قاعدة/سبب/generated/خطة/أولوية) + ديون مرتبطة |
| `.github/workflows/main.yml` | lint رُقّي إلى **hard gate** (تحقق بنيوي §8 من التوجيه) |
| `CHANGELOG.md` | مدخل Unreleased لدفعة lint-gate |

---

## 4. ما بعد يد المالك — بالترتيب الإلزامي

1. **مزامنة الفرع وإطلاق Run #120** ← ثم تعبئة `RUN_120_EVIDENCE.md §4` من `gh run view <id> --log-failed` (القالب جاهز).
2. **تصنيف أي فشل جديد** من جدول طبقات §3 في ملف الأدلة — طبقة واحدة/commit واحد.
3. **عند نجاح Stage 2:** `5.json` يصدر آليًا من KSP ⇒ يُرفق ⇒ يُشغَّل `RoomMigration4To5Test` فعليًا ⇒ يُحدَّث تقرير Room (بندا 15/17/18).
4. **Flutter:** إقرار جدول `FLUTTER_APPROVAL_MATRIX` (حزمة-حزمة أو كاملًا) ⇒ تنفيذ ملحقي A/B كـ commits مستقلة ⇒ `flutter analyze && flutter test` ⇒ عندها فقط فتح pubspec وفق ACCEPT المعتمد.
5. **بعد اكتمال 1–4:** المرحلة 1 تُعلن مكتملة بالإثبات لا بالادعاء.

---

## 5. إعلانات صريحة (قاعدة 7 من التوجيه)

**غير معلن ولا يُعلن حتى يُنفَّذ فعليًا:**
- نجاح بناء Android أو اجتياز أي اختبار Android/Room (Run #120 لم يُطلق من هنا).
- نجاح أي اختبار Flutter أو بناء APK.
- نجاح بوابة lint في CI (محليًا PASS؛ إثبات CI من Run #120).

**معلن بثبوت الأدلة المحلية:** سلامة web typecheck + lint (exit 0)، بوابة lint الصلبة بنيويًا، مطابقة السكربت الأندرويدي للإصلاح، عدم وجود مراجل معلقة بعد حذف الكود الميت.

---

## 6. حدود التوقف (ملتزم بها من التوجيه)

- ❌ لا دمج في main · ❌ لا release-mobile · ❌ لا وكلاء ذكاء اصطناعي أو طبقة LegalTech
- ❌ لا تعديل `pubspec.yaml` · ❌ لا `5.json` يدوي · ❌ لا force push ولا مفاتيح توقيع
- ✅ التقدم التالي المشروط فقط: Run #120 / موافقة جدول Flutter
